server:
  port: 8080
spring:
  application:
    name: stock-analysis-api
  datasource:
    url: jdbc:mysql://localhost:3306/mytestdb2?useSSL=false
    username: root
    password: sathishtest123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        sql: DEBUG
        type: TRACE